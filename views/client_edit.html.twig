{% include 'header.html.twig' %}
            <br>
            <h4>Update Info on {{ client.getName }}?</h4>
            <div class="edit-form">
                <form action="/clients/{{ client.getId }}" method="post">
                    <input name="_method" type="hidden" value="patch">
                    <label for="name">Rename {{ client.getName }}</label>
                    <input id="name" name="name" type="text">
                    <button class="btn btn-success" type="submit">Update</button>
                </form>
                <br>
                <p>Shy Away from this Danger! <a href="/">Go Back!</a></p>
                <br>
                <hr>
                <form action="/clients/{{ client.getId }}" method="post">
                    <input name="_method" type="hidden" value="delete">
                    <button class="btn btn-danger" type="submit">Delete this Client</button>
                </form>
                <hr>
                <p><a href="/">Back to Home</a></p>
            </div>

{% include 'footer.html.twig' %}
